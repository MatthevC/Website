document.querySelector('#eventForm').addEventListener('submit',async e=>{e.preventDefault();const f=new FormData(e.target);const {error}=await supabaseClient.from('events').insert([{title:f.get('title'),start_date:f.get('start_date'),end_date:f.get('end_date'),description:f.get('description'),image_url:f.get('image_url')}]);msg.innerText=error?error.message:'Dodano event';if(!error)e.target.reset();});